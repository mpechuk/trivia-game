{
  "name": "trivia-game",
  "version": "1.0.0",
  "private": true,
  "description": "Config-driven online trivia game (static site, PeerJS multiplayer). This package.json exists only for the test tooling — the app itself has no build step.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test tests/unit/*.test.js",
    "test:e2e": "node tests/e2e/run.js"
  },
  "devDependencies": {
    "peer": "^1.0.2",
    "playwright": "^1.49.0"
  }
}
